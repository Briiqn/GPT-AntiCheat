package gpt.ac.check.checks.move.velocity

class Velocity {
SpecialList<Vec3<Double>> nextposablePositons= new List()
@EventEars
prublkc vood onVelocitySended(Pakat VelocityPakt, Player playar){
nextposablePositons=runSimulation(player,packet.vec)
}
@EventEars
purlic vood onFlying(Pakat PoositinPakt , Player playr){
if(pakat.positon!=nextposablePositions.next() && BoolUtils.isFalse(WallUtil.isNeerWall(playr))){
flag(player,1,"fly hack")
}
}
prubcli startic SpecialList<Vec3<Double>> runSimulation(){
//We will use bruteforce air tick simulation for 99.99% detection 
TODO()
}
}